{
	// Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"Machine_Learning": {
		"prefix": "ml",
		"body": [
		"import numpy as np",
		"import matplotlib.pyplot as plt",
		"from sklearn.model_selection import train_test_split",
		"from sklearn.linear_model import LinearRegression",
		"from sklearn.metrics import mean_squared_error, r2_score",
		"ds = ds.drop(columns=['one'])",
		"print(ds.head())",
		"x = ds.iloc[:, :-1]",
		"y = ds.iloc[:, -1]",
		"x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.25, random_state=0)",
		"model = LinearRegression()",
		"model.fit(x_train, y_train)",
		"y_pred = model.predict(x_test)",
		"mse = mean_squared_error(y_test, y_pred)",
		"r2 = r2_score(y_test, y_pred)",
		"print(, mse)",
		"print(, r2)",
		"plt.figure(figsize=(8, 6))  ",
		"plt.scatter(y_test, y_test, color = 'blue', alpha=0.5)",
		"plt.scatter(y_test, y_pred, color = 'red', alpha=0.5)",
		"plt.plot([y_test.min(),y_test.max()], [y_test.min(),y_test.max()], color = 'black', linestyle = 'dashed')",
		"plt.title('Actual vs Predicted CO2 Emission ')",
		"plt.xlabel('Actual CO2 Emission')",
		"plt.ylabel('Predicted CO2 Emission')",
		"plt.tight_layout()",
		"plt.show()",
		],
		"description": "can plot actual and model predicted data points for the entire dataset"
	},
	"Machine_learning": {
		"prefix": "rd csv",
		"body": [
			"import pandas as pd",
			"df = pd.read_csv(r'$1');",
			"print(df.head())",
			"$2"
		],
		"description": "read the dataset"
	},
	"machine_learning": {
		"prefix": "comment",
		"body": [
		"#question",
		"",
		"# Read the csv file",
		"$1",
		"# Feature Engineering",
		"$2",
		"# Select Features and Target",
		"$3",
		"#Train-test data split",
		"$4",
		"# Model training",
		"$5",
		"# check error ",
		"$6",
		"# Graph plot",
		"$7",
		"",
		"$8",
		],
		"description": "see the steps"
	}
}